let Header = () => {
  return (
    <header className="app-header">
      <h1>Todo Dashboard</h1>
      <p>Track your work with priority, deadlines and status.</p>
    </header>
  );
};

export default Header;
